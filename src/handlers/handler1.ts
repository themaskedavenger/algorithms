/**
 * @file Enmesh module handler.
 */

import { Enmesh } from '@enmesh/core';

export default async function handler1(
  enmesh: Enmesh,
  emit,
  ...emitterParams: any[]
): Promise<any> {
  // emitter function
}
