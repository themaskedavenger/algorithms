/**
 * @file Selection sorter class.
 */
import Sorter from './Sorter';
export default class ShellSorter extends Sorter {
    static hSort(a: number[], h: number): void;
    static sort(a: number[]): void;
}
